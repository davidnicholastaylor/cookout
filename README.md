# Practice with Iterating through Arrays via Functions

## Objective: 
#### Iterate through an array, changing a boolean value from true to false without changing the function provided.

## Solution:
#### Call the function from within a for loop that iterates through the array, using the array with an index of the iteration as the argument.
